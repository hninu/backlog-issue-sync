# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
pre-commit:
  parallel: true
  jobs:
    - run: npx biome check {staged_files} --write
      glob: "*.{js,ts,jsx,tsx,json}"
      stage_fixed: true
